#15 presto 설정

$ presto-cli-0.219-executable --server localhost:8889 --catalog hive --schema njkwon
presto:default> show catalogs;
presto:default> show schemas from hive;
presto:default> show tables from hive.spark;
presto:default> select * from hive.spark.dp1_danji_user_view;

#16 zeppelin jdbc interprinter 설치
$ sudo ./bin/install-interpreter.sh --name jdbc
$ sudo ./bin/zeppelin-daemon.sh restart


#19
default.driver : com.facebook.presto.jdbc.PrestoDriver
default.url    : jdbc:presto://localhost:8889
default.user   : hive

Dependencies
com.facebook.presto:presto-jdbc:0.179 

#47

PUT _template/template_3
{
  "index_patterns": [ "sample*","njkwon*", "testjson*"],
  "mappings": {
    "doc": {
      "properties": {
        "timestamp": {
          "type": "date",
          "format": "yyyy-MM-dd HH:mm:ss"
        }, 
        "base_date": {
          "type": "date",
          "format": "yyyy-MM-dd"
        }, 
          "base_dt": {
          "type": "date",
          "format": "yyyy-MM-dd HH:mm:ss"
        } 
      }
    }
  }
} 




https://www.tableau.com/products/trial 

각자 os 맞는 tableau 를 설치한다.

odbc 설치
https://www.tableau.com/ko-kr/support/drivers

presto 와 mysql driver 설치 


